Node Exporter Ansible Playbook
====================================

Overview
------------------------------------

An Ansible playbook to setup Node Exporter (Prometheus) as a service on a remote machine

Configuration
------------------------------------

Content in `/config` can be used as guideline for host running Ansible playbook.  Place these config files in `/etc/ansible/`.  Handles SSH through jumpbox for example.